pull_request_rules:
  - name: Auto merge pull requests
    conditions:
      or:
        - author=asahiocean
        - author=dependabot[bot]
    actions:
      review:
        type: APPROVE
        message: Automatically approving
      merge:
        method: merge
