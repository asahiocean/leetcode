pull_request_rules:
   - name: Auto merge for Dependabot pull requests
     conditions:
      - author~=^dependabot(|-preview)\[bot\]$
     actions:
       review:
         type: APPROVE
         message: Automatically approving dependabot
       merge:
         method: merge
pull_request_rules:
  - name: Add "moved" label
    conditions:
      - base~=moved
    actions:
      label:
          add:
          - moved
pull_request_rules:
    - name: automatic approval pull requests
      conditions:
       - author=asahiocean
      actions:
        review:
          type: APPROVE
        merge:
          method: merge